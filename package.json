{
  "name": "vera_nutri",
  "version": "1.0.0",
  "description": "Personalized Nutrition Plans for a Healthier You.",
  "source": "public/index.html",
  "scripts": {
    "start": "parcel --dist-dir build --port 3000 --open",
    "build": "parcel build --dist-dir build"
  },
  "license": "MIT",
  "dependencies": {
    "preact": "^10.11.2",
    "preact-compat": "^3.19.0"
  },
  "devDependencies": {
    "classnames": "^2.5.1",
    "parcel": "^2.7.0",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "alias": {
    "react": "preact-compat",
    "react-dom$": "preact-compat",
    "react-dom/server": "preact-compat/server",
    "preact/jsx-dev-runtime": "preact/jsx-runtime"
  }
}
